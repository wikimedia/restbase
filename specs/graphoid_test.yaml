paths:
  /v1/png/{title}/{revision}/{graph_id}:
      get:
        x-request-handler:
          - get_from_graphoid:
              request:
                uri: '{+$$.options.host}/{domain}/v1/png/{title}/{revision}/{graph_id}'

paths:
  /key_value:
    x-modules:
      - path: sys/backend_proxy.js
        options: '{{options.proxy}}'
  /page_revisions:
    x-modules:
      - path: sys/backend_proxy.js
        options: '{{options.proxy}}'


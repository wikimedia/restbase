paths:
  /table:
    x-modules:
      - path: sys/table.js
        options:
          conf: '{{options.table}}'
  /action:
    x-modules:
      - path: sys/action.js
        options: "{{options.action}}"
  /key_value:
    x-modules:
      - path: sys/key_value.js
  /page_revisions:
    x-modules:
      - path: sys/page_revisions.js
